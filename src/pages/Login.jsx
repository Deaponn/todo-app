import styled from "styled-components"
import Wrapper from "../components/Wrapper"

const Window = styled.div`
    
`

export default function Login(){
    return <Wrapper center><div>login</div></Wrapper>
}